function selectFighter() {

    let sign = prompt("Select your fighters: cute or chads")

    while (sign.toLocaleLowerCase() != "cute") {
        let sign = prompt("Select your fighters: cute or chads")

    }
}